<?xml version="1.0" encoding="GBK"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><!-- create by autoCode,v1.0.0 --><mapper namespace="front.attribute">	<select id="front.attribute.selectPageList" parameterType="net.jeeshop.services.front.attribute.bean.Attribute" resultType="net.jeeshop.services.front.attribute.bean.Attribute">	 select t.*,(select c.name from t_catalog c where c.id=t.catalogID) catalogName 	 from t_attribute t where 1=1  and pid=#{pid} 	 <if test="catalogID!=0">	 	and catalogID=#{catalogID} 	 </if> order by id desc  limit #{offset},#{pageSize} 	</select> 	<select id="front.attribute.selectPageCount" parameterType="net.jeeshop.services.front.attribute.bean.Attribute" resultType="java.lang.Integer">	 select count(*) from (select 1 	 from t_attribute t where 1=1 and pid=#{pid} 	 <if test="catalogID!=0">	 	and catalogID=#{catalogID} 	 </if>	 ) a 	</select> 	<insert id="front.attribute.insert" parameterType="net.jeeshop.services.front.attribute.bean.Attribute" useGeneratedKeys="true" keyProperty="id">		insert into t_attribute		(		id,catalogID		<if test="name!=null and name!=''">		,name		</if>		<if test="pid!=0">		,pid		</if>		)		values		(		id,#{catalogID}		<if test="name !=null and name !=''">		,#{name}		</if>		<if test="pid !=0">		,#{pid}		</if>	)</insert>		<select id="front.attribute.selectList" parameterType="net.jeeshop.services.front.attribute.bean.Attribute" resultType="net.jeeshop.services.front.attribute.bean.Attribute">	 select * from t_attribute t where 1=1 and pid!=-1	</select>		<update id="front.attribute.update" parameterType="net.jeeshop.services.front.attribute.bean.Attribute">		update t_attribute		  set id=#{id}		<if test="name !=null and name !=''">		,name=#{name}		</if>		<if test="catalogID !=0">		,catalogID=#{catalogID}		</if>		<if test="pid !=0">		,pid=#{pid}		</if>		 where 1=1 and id=#{id}	</update>	<select id="front.attribute.selectOne" parameterType="net.jeeshop.services.front.attribute.bean.Attribute" resultType="net.jeeshop.services.front.attribute.bean.Attribute">	 select * 	 from t_attribute t where 1=1  	 <if test="id!=null and id!=''">	 	and id=#{id}	 </if>	 <if test="pid!=0">	 	and pid=#{pid}	 </if>	 <if test="catalogID!=0">	 	and catalogID=#{catalogID}	 </if>	</select>	<delete id="front.attribute.deleteById" parameterType="net.jeeshop.services.front.attribute.bean.Attribute">		delete from t_attribute where 1=1 and id=#{id}	</delete>	<delete id="front.attribute.deleteByPid" parameterType="net.jeeshop.services.front.attribute.bean.Attribute">		delete from t_attribute where 1=1 and pid=#{id}	</delete></mapper>