<?xml version="1.0" encoding="GBK"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><!-- create by autoCode,v1.0.0 --><mapper namespace="front.catalog">	<select id="front.catalog.selectPageList" parameterType="net.jeeshop.services.front.catalog.bean.Catalog" resultType="net.jeeshop.services.front.catalog.bean.Catalog">	 select * 	 from t_catalog t where 1=1  order by order1  limit #{offset},#{pageSize} 	</select> 	<select id="front.catalog.selectPageCount" parameterType="net.jeeshop.services.front.catalog.bean.Catalog" resultType="java.lang.Integer">	 select count(*) from t_catalog where 1=1 	</select> 	<select id="front.catalog.selectOne" parameterType="net.jeeshop.services.front.catalog.bean.Catalog" resultType="net.jeeshop.services.front.catalog.bean.Catalog">	 select * 	 from t_catalog t where 1=1 	 <if test="id!=null and id!=''"> and id=#{id}</if>	 <if test="code!=null and code!=''"> and code=#{code}</if>	</select>		<select id="front.catalog.selectList" parameterType="net.jeeshop.services.front.catalog.bean.Catalog" resultType="net.jeeshop.services.front.catalog.bean.Catalog">	 select * 	 from t_catalog t where 1=1 	 <if test="pid!=null and pid!=''">		 and pid=#{pid}	 </if>	 <if test="type!=null and type!=''">		 and type=#{type} 	 </if>	 order by order1	</select></mapper>