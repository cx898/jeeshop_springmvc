<?xml version="1.0" encoding="GBK"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><!-- create by autoCode,v1.0.0 --><mapper namespace="front.express">	<select id="front.express.selectPageList" parameterType="net.jeeshop.services.front.express.bean.Express" resultType="net.jeeshop.services.front.express.bean.Express">	 select * 	 from t_express t where 1=1  order by order1   limit #{offset},#{pageSize} 	</select> 	<select id="front.express.selectPageCount" parameterType="net.jeeshop.services.front.express.bean.Express" resultType="java.lang.Integer">	 select count(*) from (select 1 	 from t_express t where 1=1 	 ) a 	</select>		<select id="front.express.selectList" parameterType="net.jeeshop.services.front.express.bean.Express" resultType="net.jeeshop.services.front.express.bean.Express">	 select * from t_express t where 1=1 order by order1 	</select> 	<insert id="front.express.insert" parameterType="net.jeeshop.services.front.express.bean.Express" useGeneratedKeys="true" keyProperty="id">		insert into t_express		(		id		<if test="code!=null and code!=''">		,code		</if>		<if test="name!=null and name!=''">		,name		</if>		<if test="fee!=null and fee!=''">		,fee		</if>		<if test="order1!=0">		,order1		</if>		)		values		(		id		<if test="code !=null and code !=''">		,#{code}		</if>		<if test="name !=null and name !=''">		,#{name}		</if>		<if test="fee !=null and fee !=''">		,#{fee}		</if>		<if test="order1 !=0">		,#{order1}		</if>	)</insert>	<update id="front.express.update" parameterType="net.jeeshop.services.front.express.bean.Express">		update t_express		  set id=#{id}		<if test="code !=null and code !=''">		,code=#{code}		</if>		<if test="name !=null and name !=''">		,name=#{name}		</if>		<if test="fee !=null and fee !=''">		,fee=#{fee}		</if>		<if test="order1 !=0">		,order1=#{order1}		</if>		 where 1=1 and id=#{id}	</update>	<select id="front.express.selectOne" parameterType="net.jeeshop.services.front.express.bean.Express" resultType="net.jeeshop.services.front.express.bean.Express">	 select * from t_express t where 1=1 and id=#{id}	</select>	<delete id="front.express.deleteById" parameterType="net.jeeshop.services.front.express.bean.Express">		delete from t_express where 1=1 and id=#{id}	</delete></mapper>