<?xml version="1.0" encoding="GBK"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><!-- create by autoCode,v1.0.0 --><mapper namespace="front.favorite">	<select id="front.favorite.selectPageList" parameterType="net.jeeshop.services.front.favorite.bean.Favorite" resultType="net.jeeshop.services.front.favorite.bean.Favorite">	 select * from t_favorite t where 1=1 	 <if test="account!=null and account!=''"> and account=#{account} </if>  order by id desc  limit #{offset},#{pageSize} 	</select> 	<select id="front.favorite.selectPageCount" parameterType="net.jeeshop.services.front.favorite.bean.Favorite" resultType="java.lang.Integer">	 select count(*) from t_favorite where 1=1 <if test="account!=null and account!=''"> and account=#{account} </if>	</select> 	<insert id="front.favorite.insert" parameterType="net.jeeshop.services.front.favorite.bean.Favorite" useGeneratedKeys="true" keyProperty="id">		insert into t_favorite		(		id,createtime		<if test="account!=null and account!=''">		,account		</if>		<if test="productID!=null and productID!=''">		,productID		</if>		)		values		(		id,now()		<if test="account !=null and account !=''">		,#{account}		</if>		<if test="productID !=null and productID !=''">		,#{productID}		</if>	)</insert>		<update id="front.favorite.update" parameterType="net.jeeshop.services.front.favorite.bean.Favorite">		update t_favorite		  set id=#{id}		<if test="account !=null and account !=''">		,account=#{account}		</if>		<if test="productID !=null and productID !=''">		,productID=#{productID}		</if>		 where 1=1 and id=#{id}	</update>		<select id="front.favorite.selectOne" parameterType="net.jeeshop.services.front.favorite.bean.Favorite" resultType="net.jeeshop.services.front.favorite.bean.Favorite">	 select 		t.id "id",		t.account "account",		t.productID "productid",		t.createtime "createtime"	 from t_favorite t where 1=1 and id=#{id}	</select>		<select id="front.favorite.selectCount" parameterType="net.jeeshop.services.front.favorite.bean.Favorite" resultType="int">	 select count(*) from t_favorite t where 1=1 	 <if test="account!=null and account!=''"> and account=#{account} </if>	 <if test="productID!=null and productID!=''"> and productID=#{productID} </if>	</select>		<delete id="front.favorite.deleteById" parameterType="net.jeeshop.services.front.favorite.bean.Favorite">		delete from t_favorite where 1=1 and id=#{id}	</delete></mapper>