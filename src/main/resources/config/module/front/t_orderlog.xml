<?xml version="1.0" encoding="GBK"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><!-- create by autoCode,v1.0.0 --><mapper namespace="front.orderlog">	<select id="front.orderlog.selectPageList" parameterType="net.jeeshop.services.front.orderlog.bean.Orderlog" resultType="net.jeeshop.services.front.orderlog.bean.Orderlog">	 select * 	 from t_orderlog t where 1=1  order by id desc  limit #{offset},#{pageSize} 	</select> 	<select id="front.orderlog.selectPageCount" parameterType="net.jeeshop.services.front.orderlog.bean.Orderlog" resultType="java.lang.Integer">	 select count(*) from (select 1 	 from t_orderlog t where 1=1 	 ) a 	</select>		<select id="front.orderlog.selectList" parameterType="net.jeeshop.services.front.orderlog.bean.Orderlog" resultType="net.jeeshop.services.front.orderlog.bean.Orderlog">	 select * 	 from t_orderlog t where 1=1 	 <if test="orderid!=null and orderid!=''"> and orderid=#{orderid}</if> order by id 	</select> 	<insert id="front.orderlog.insert" parameterType="net.jeeshop.services.front.orderlog.bean.Orderlog" useGeneratedKeys="true" keyProperty="id">		insert into t_orderlog		(		id,createdate,orderid		<if test="account!=null and account!=''">		,account		</if>		<if test="content!=null and content!=''">		,content		</if>		<if test="accountType!=null and accountType!=''">		,accountType		</if>		)		values		(		id,now(),#{orderid}		<if test="account !=null and account !=''">		,#{account}		</if>		<if test="content !=null and content !=''">		,#{content}		</if>		<if test="accountType!=null and accountType!=''">		,#{accountType}		</if>	)</insert>		<select id="front.orderlog.selectOne" parameterType="net.jeeshop.services.front.orderlog.bean.Orderlog" resultType="net.jeeshop.services.front.orderlog.bean.Orderlog">	 select * 	 from t_orderlog t where 1=1 and id=#{id}	</select>		<select id="front.orderlog.selectCount" parameterType="net.jeeshop.services.front.orderlog.bean.Orderlog" resultType="int">	 select count(*) from t_orderlog t where 1=1 	 <if test="account!=null and account!=''"> and account=#{account} </if>	 <if test="content!=null and content!=''"> and content=#{content} </if>	</select>	</mapper>