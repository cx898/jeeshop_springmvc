<?xml version="1.0" encoding="GBK"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><!-- create by autoCode,v1.0.0 --><mapper namespace="front.pay">	<select id="front.pay.selectPageList" parameterType="net.jeeshop.services.front.pay.bean.Pay" resultType="net.jeeshop.services.front.pay.bean.Pay">	 select * 	 from t_pay t where 1=1  order by order1   limit #{offset},#{pageSize} 	</select> 	<select id="front.pay.selectPageCount" parameterType="net.jeeshop.services.front.pay.bean.Pay" resultType="java.lang.Integer">	 select count(*) from t_pay t where 1=1 	</select> 	<select id="front.pay.selectOne" parameterType="net.jeeshop.services.front.pay.bean.Pay" resultType="net.jeeshop.services.front.pay.bean.Pay">	 select * from t_pay t where 1=1  and status='y' 	 <if test="id!=null and id!=''"> and id=#{id} </if> 	 <if test="code!=null and code!=''"> and code=#{code} </if> 	</select></mapper>