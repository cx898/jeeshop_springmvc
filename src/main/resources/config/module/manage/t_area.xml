<?xml version="1.0" encoding="GBK"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><!-- create by autoCode,v1.0.0 --><mapper namespace="manage.area">	<select id="manage.area.selectPageList" parameterType="net.jeeshop.services.manage.area.bean.Area" resultType="net.jeeshop.services.manage.area.bean.Area">	 select * 	 from t_area t where 1=1  order by id desc  limit #{offset},#{pageSize} 	</select> 	<select id="manage.area.selectPageCount" parameterType="net.jeeshop.services.manage.area.bean.Area" resultType="java.lang.Integer">	 select count(*) from (select 1 	 from t_area t where 1=1 	 ) a 	</select>		<select id="manage.area.selectList" parameterType="net.jeeshop.services.manage.area.bean.Area" resultType="net.jeeshop.services.manage.area.bean.Area">	 select * from t_area t where 1=1 <if test="pcode!=null and pcode!=''"> and pcode=#{pcode} </if>  	 order by code 	</select>		<select id="manage.area.selectListByPcode" parameterType="string" resultType="net.jeeshop.services.manage.area.bean.Area">	 select * from t_area t where 1=1 and pcode=#{pcode} order by code	</select>		<select id="manage.area.getCount" parameterType="net.jeeshop.services.manage.area.bean.Area" resultType="int">	 select count(*) c from t_area t where 1=1 	 <if test="code!=null and code!=''"> and code=#{code} </if>	 <if test="pcode!=null and pcode!=''"> and pcode=#{pcode} </if>	</select> 	<insert id="manage.area.insert" parameterType="net.jeeshop.services.manage.area.bean.Area" useGeneratedKeys="true" keyProperty="id">		insert into t_area		(		id		<if test="code!=null and code!=''">		,code		</if>		<if test="name!=null and name!=''">		,name		</if>		<if test="pcode!=null and pcode!=''">		,pcode		</if>		<if test="desc1!=null and desc1!=''">		,desc1		</if>		)		values		(		id		<if test="code!=null and code!=''">		,#{code}		</if>		<if test="name!=null and name!=''">		,#{name}		</if>		<if test="pcode!=null and pcode!=''">		,#{pcode}		</if>		<if test="desc1 !=null and desc1 !=''">		,#{desc1}		</if>	)</insert>	<update id="manage.area.update" parameterType="net.jeeshop.services.manage.area.bean.Area">		update t_area		  set id=#{id}		<if test="code!=null and code!=''">		,code=#{code}		</if>		<if test="name!=null and name!=''">		,name=#{name}		</if>		<if test="pcode!=null and pcode!=''">		,pcode=#{pcode}		</if>		<if test="desc1 !=null and desc1 !=''">		,desc1=#{desc1}		</if>		 where 1=1 and id=#{id}	</update>	<select id="manage.area.selectOne" parameterType="net.jeeshop.services.manage.area.bean.Area" resultType="net.jeeshop.services.manage.area.bean.Area">	 select * from t_area t where 1=1 and id=#{id}	</select>	<delete id="manage.area.deleteById" parameterType="net.jeeshop.services.manage.area.bean.Area">		delete from t_area where 1=1 and id=#{id}	</delete>		<delete id="manage.area.deleteAll">		delete from t_area where 1=1 	</delete>		<delete id="manage.area.delete" parameterType="net.jeeshop.services.manage.area.bean.Area">		delete from t_area where code=#{code}	</delete></mapper>