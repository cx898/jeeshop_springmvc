<?xml version="1.0" encoding="GBK"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><!-- create by autoCode,v1.0.0 --><mapper namespace="manage.task">	<select id="manage.task.selectPageList" parameterType="net.jeeshop.services.manage.task.bean.Task" resultType="net.jeeshop.services.manage.task.bean.Task">	 select * from t_task t where 1=1  order by id desc  limit #{offset},#{pageSize} 	</select> 	<select id="manage.task.selectPageCount" parameterType="net.jeeshop.services.manage.task.bean.Task" resultType="java.lang.Integer">	 select count(*) from (select 1 	 from t_task t where 1=1 	 ) a 	</select>		<select id="manage.task.selectList" parameterType="net.jeeshop.services.manage.task.bean.Task" resultType="net.jeeshop.services.manage.task.bean.Task">	 select * from t_task t where 1=1  order by id desc 	</select> 	<insert id="manage.task.insert" parameterType="net.jeeshop.services.manage.task.bean.Task" useGeneratedKeys="true" keyProperty="id">		insert into t_task		(		id		<if test="code!=null and code!=''">		,code		</if>		<if test="name!=null and name!=''">		,name		</if>		<if test="sleep!=null and sleep!=''">		,sleep		</if>		<if test="unit!=null and unit!=''">		,unit		</if>		<if test="nextWorkTime!=null and nextWorkTime!=''">		,nextWorkTime		</if>		<if test="currentStatus!=null and currentStatus!=''">		,currentStatus		</if>		)		values		(		id		<if test="code !=null and code !=''">		,#{code}		</if>		<if test="name !=null and name !=''">		,#{name}		</if>		<if test="sleep !=null and sleep !=''">		,#{sleep}		</if>		<if test="unit !=null and unit !=''">		,#{unit}		</if>		<if test="nextWorkTime !=null and nextWorkTime !=''">		,#{nextWorkTime}		</if>		<if test="currentStatus !=null and currentStatus !=''">		,#{currentStatus}		</if>	)</insert>	<update id="manage.task.update" parameterType="net.jeeshop.services.manage.task.bean.Task">		update t_task		  set id=#{id}		<if test="code !=null and code !=''">		,code=#{code}		</if>		<if test="name !=null and name !=''">		,name=#{name}		</if>		<if test="sleep !=null and sleep !=''">		,sleep=#{sleep}		</if>		<if test="unit !=null and unit !=''">		,unit=#{unit}		</if>		<if test="nextWorkTime !=null and nextWorkTime !=''">		,nextWorkTime=#{nextWorkTime}		</if>		<if test="currentStatus !=null and currentStatus !=''">		,currentStatus=#{currentStatus}		</if>		 where 1=1 and id=#{id}	</update>	<select id="manage.task.selectOne" parameterType="net.jeeshop.services.manage.task.bean.Task" resultType="net.jeeshop.services.manage.task.bean.Task">	 select * from t_task t where 1=1 and id=#{id}	</select>	<delete id="manage.task.deleteById" parameterType="net.jeeshop.services.manage.task.bean.Task">		delete from t_task where 1=1 and id=#{id}	</delete></mapper>